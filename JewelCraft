<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Troy Clancy Jewellery • JewelCraft</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.min.js"></script>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');
    body { font-family: 'Inter', system-ui, sans-serif; }
  </style>
</head>
<body class="bg-zinc-950 text-white min-h-screen">

  <!-- PASSWORD SCREEN -->
  <div id="password-screen" class="fixed inset-0 bg-zinc-950 flex items-center justify-center z-50">
    <div class="bg-zinc-900 rounded-3xl p-12 w-full max-w-md text-center">
      <div class="text-5xl mb-6">💎</div>
      <h1 class="text-3xl font-bold mb-2">JewelCraft Insights</h1>
      <p class="text-amber-400 mb-8">Troy Clancy Jewellery • Private</p>
      
      <input id="password-input" type="password" placeholder="Enter Password" 
             class="w-full bg-zinc-800 text-center rounded-2xl p-5 text-lg mb-6 focus:outline-none focus:ring-2 focus:ring-amber-400">
      
      <button onclick="checkPassword()" 
              class="w-full bg-amber-400 text-zinc-950 font-semibold py-5 rounded-3xl text-lg">
        UNLOCK DASHBOARD
      </button>
      <p id="error-msg" class="text-red-400 mt-4 text-sm hidden">Incorrect password</p>
    </div>
  </div>

  <!-- MAIN APP -->
  <div id="main-app" class="hidden flex h-screen">
    <div class="w-72 bg-zinc-900 border-r border-zinc-800 flex flex-col">
      <div class="p-6 border-b border-zinc-800">
        <div class="flex items-center gap-3">
          <div class="w-9 h-9 bg-gradient-to-br from-amber-400 to-yellow-600 rounded-xl flex items-center justify-center text-xl">💎</div>
          <div>
            <h1 class="text-2xl font-bold tracking-tight">JewelCraft</h1>
            <p class="text-amber-400 text-sm">Troy Clancy Jewellery</p>
          </div>
        </div>
      </div>
      <nav class="flex-1 p-4 space-y-1" id="nav"></nav>
      <div class="p-4 border-t border-zinc-800 text-xs text-zinc-500">
        Private • Local Data • v3.0
      </div>
    </div>

    <div class="flex-1 flex flex-col">
      <header class="h-16 bg-zinc-900 border-b border-zinc-800 px-8 flex items-center justify-between">
        <h2 id="page-title" class="text-2xl font-semibold">Dashboard</h2>
        <div class="flex items-center gap-6">
          <div class="flex items-center gap-2 text-sm">
            <div class="w-3 h-3 bg-emerald-400 rounded-full animate-pulse"></div>
            <span class="text-emerald-400">Secured</span>
          </div>
          <div class="text-right">
            <div class="font-medium">Troy Clancy</div>
            <div class="text-xs text-zinc-500">Master Jeweller</div>
          </div>
        </div>
      </header>
      <div class="flex-1 overflow-auto p-8" id="main-content"></div>
    </div>
  </div>

  <script>
    const CORRECT_PASSWORD = "c99#1@axWTE!NH^B";   // ← Your new secure password

    function checkPassword() {
      if (document.getElementById('password-input').value === CORRECT_PASSWORD) {
        document.getElementById('password-screen').classList.add('hidden');
        document.getElementById('main-app').classList.remove('hidden');
        buildNav();
        pages.dashboard.render();
      } else {
        document.getElementById('error-msg').classList.remove('hidden');
      }
    }

    // ==================== PAGES ====================
    const pages = {
      dashboard: { title: "Dashboard", icon: "📊", render: renderDashboard },
      crm: { title: "Client Vault", icon: "👥", render: renderCRM },
      playbook: { title: "Outperform Playbook", icon: "🚀", render: renderPlaybook },
      analytics: { title: "Analytics", icon: "📈", render: renderAnalytics },
      logger: { title: "Content Logger", icon: "📝", render: renderContentLogger },
      calculator: { title: "Cost Calculator", icon: "🧮", render: renderCalculator },
      competitors: { title: "Competitors", icon: "🏆", render: renderCompetitors }
    };

    function buildNav() {
      const nav = document.getElementById('nav');
      nav.innerHTML = '';
      Object.keys(pages).forEach(key => {
        const p = pages[key];
        const el = document.createElement('div');
        el.className = `flex items-center gap-3 px-4 py-3 rounded-2xl cursor-pointer hover:bg-zinc-800 ${key === 'dashboard' ? 'bg-zinc-800' : ''}`;
        el.innerHTML = `<span class="text-xl">${p.icon}</span><span>${p.title}</span>`;
        el.onclick = () => {
          document.querySelectorAll('#nav > div').forEach(d => d.classList.remove('bg-zinc-800'));
          el.classList.add('bg-zinc-800');
          document.getElementById('page-title').textContent = p.title;
          p.render();
        };
        nav.appendChild(el);
      });
    }

    function renderDashboard() {
      document.getElementById('main-content').innerHTML = `
        <div class="max-w-6xl mx-auto text-center py-20">
          <h1 class="text-5xl font-bold text-amber-400 mb-6">Welcome back, Troy</h1>
          <p class="text-xl text-zinc-400">Your secure private dashboard is ready.</p>
        </div>`;
    }

    // Placeholder pages (you can expand these later)
    function renderCRM() { document.getElementById('main-content').innerHTML = `<div class="text-center py-20 text-2xl">👥 Client Vault</div>`; }
    function renderPlaybook() { document.getElementById('main-content').innerHTML = `<div class="text-center py-20 text-2xl">🚀 Outperform Playbook</div>`; }
    function renderAnalytics() { document.getElementById('main-content').innerHTML = `<div class="text-center py-20 text-2xl">📈 Analytics Tracker</div>`; }
    function renderContentLogger() { document.getElementById('main-content').innerHTML = `<div class="text-center py-20 text-2xl">📝 Content Logger</div>`; }
    function renderCalculator() { document.getElementById('main-content').innerHTML = `<div class="text-center py-20 text-2xl">🧮 Cost Calculator</div>`; }
    function renderCompetitors() { document.getElementById('main-content').innerHTML = `<div class="text-center py-20 text-2xl">🏆 Competitor Intel</div>`; }

    // Enter key support
    document.getElementById('password-input').addEventListener('keypress', e => {
      if (e.key === 'Enter') checkPassword();
    });
  </script>
</body>
</html>
